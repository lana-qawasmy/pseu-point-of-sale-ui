import 'item.css';
import { ItemNS } from '../../types';

interface IProps {
    item: ItemNS.Item;
}

const Item = (props: IProps) => {
    return (
        <div>

        </div>
    );
};

export default Item;